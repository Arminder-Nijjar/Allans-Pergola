services:
  - type: web
    name: pergola-builder-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: TO_EMAILS
        value: narminder1@gmail.com
      - key: FROM_EMAIL
        value: noreply@allanslandscaping.ca
      - key: PYTHON_VERSION
        value: 3.11.0
